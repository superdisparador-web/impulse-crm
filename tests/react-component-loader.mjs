import {readFileSync,existsSync} from 'node:fs';import path from 'node:path';import ts from 'typescript';import {createRequire} from 'node:module';
const nativeRequire=createRequire(import.meta.url),cache=new Map(),root=process.cwd();
function resolveLocal(request,parent){const base=request.startsWith('@/')?path.join(root,request.slice(2)):request.startsWith('.')?path.resolve(path.dirname(parent),request):null;if(!base)return null;for(const candidate of [base,`${base}.tsx`,`${base}.ts`,`${base}.js`,path.join(base,'index.tsx'),path.join(base,'index.ts')])if(existsSync(candidate))return candidate;return null}
export function loadComponent(file,mocks={}){const absolute=path.resolve(root,file);function load(target){if(cache.has(target))return cache.get(target).exports;const record={exports:{}};cache.set(target,record);const source=readFileSync(target,'utf8'),code=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020,jsx:ts.JsxEmit.ReactJSX,esModuleInterop:true}}).outputText;const localRequire=request=>{if(request in mocks)return mocks[request];const local=resolveLocal(request,target);return local?load(local):nativeRequire(request)};Function('require','module','exports',code)(localRequire,record,record.exports);return record.exports}return load(absolute)}
