export type AudienceMode='CSV'|'SEGMENT'|'MANUAL';
export type NavigationContext={step:number;audienceMode:AudienceMode;audienceValid:boolean;listConfirmed:boolean;segmentMaterialized:boolean;templateAvailable:boolean;mappingsValid:boolean;destinationConfigured:boolean;reviewed:boolean};
export function nextStep(step:number,total=7){return Math.min(total,step+1)}
export function previousStep(step:number){return Math.max(1,step-1)}
export function completedStepTarget(current:number,target:number){return target>=1&&target<=current?target:current}
export function campaignContinueReason(context:NavigationContext){if(context.step===2){if(context.audienceMode==='MANUAL'&&(!context.audienceValid||!context.listConfirmed))return'Adicione pelo menos um destinatário válido.';if(context.audienceMode==='CSV'&&(!context.audienceValid||!context.listConfirmed))return'Importe e confirme ao menos um destinatário válido.';if(context.audienceMode==='SEGMENT'&&!context.segmentMaterialized)return'Confirme a estimativa do público segmentado.'}if(context.step===3){if(!context.templateAvailable)return'Selecione um template aprovado.';if(!context.mappingsValid)return'Preencha todas as variáveis obrigatórias.'}if(context.step===4&&!context.destinationConfigured)return'Configure o destino da campanha.';if(context.step===5&&!context.reviewed)return'Confirme a revisão da campanha.';return undefined}
