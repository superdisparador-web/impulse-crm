import { ReactNode } from 'react';
export default function AudienceStep({children}:{children:ReactNode}){return <section aria-labelledby="campaign-audience-title"><h2 id="campaign-audience-title" className="sr-only">Público</h2>{children}</section>}
