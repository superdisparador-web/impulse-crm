"use client";
import { AlertCircle, CheckCircle2, LoaderCircle } from "lucide-react";
import Button from "@/components/ui/Button";
export type DraftState="saved"|"saving"|"error"|"dirty";
export default function CampaignStickyFooter({state,lastSavedAt,step,busy,continueReason,onClose,onSave,onBack,onContinue,finalLabel}:{state:DraftState;lastSavedAt?:Date;step:number;busy:boolean;continueReason?:string;onClose:()=>void;onSave:()=>void;onBack:()=>void;onContinue:()=>void;finalLabel?:string}){const meta=state==="saving"?{icon:<LoaderCircle className="animate-spin text-blue-600" size={18}/>,label:"Salvando..."}:state==="error"?{icon:<AlertCircle className="text-red-600" size={18}/>,label:"Erro ao salvar"}:state==="dirty"?{icon:<AlertCircle className="text-amber-600" size={18}/>,label:"Alterações pendentes"}:{icon:<CheckCircle2 className="text-emerald-600" size={18}/>,label:"Salvo automaticamente"};return <footer className="fixed inset-x-0 bottom-0 z-30 border-t border-slate-200/90 bg-white/90 px-4 pb-[max(.75rem,env(safe-area-inset-bottom))] pt-3 shadow-[0_-12px_32px_-18px_rgba(15,23,42,0.28)] backdrop-blur-xl"><div className="mx-auto flex max-w-7xl flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div aria-live="polite" className="flex min-w-44 items-center gap-2.5 text-sm text-slate-600">{meta.icon}<div><strong className="block font-semibold text-slate-800">{meta.label}</strong>{lastSavedAt&&state==="saved"&&<span className="text-xs text-slate-500">Última gravação às {lastSavedAt.toLocaleTimeString("pt-BR",{hour:"2-digit",minute:"2-digit"})}</span>}</div></div><div className="flex min-w-0 flex-col gap-2 sm:items-end">{continueReason&&<span id="campaign-continue-reason" role="status" className="text-xs font-medium text-amber-700">{continueReason}</span>}<div className="flex flex-wrap items-center justify-end gap-2"><Button variant="ghost" onClick={onClose}>Fechar</Button><Button variant="secondary" loading={state==="saving"} disabled={busy} onClick={onSave}>Salvar rascunho</Button><Button variant="outline" disabled={step===1||busy} onClick={onBack}>Voltar</Button><Button aria-describedby={continueReason?"campaign-continue-reason":undefined} disabled={busy||!!continueReason} loading={busy} onClick={onContinue}>{finalLabel||"Continuar"}</Button></div></div></div></footer>}
