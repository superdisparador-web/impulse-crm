const field="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-slate-900 shadow-sm focus:border-blue-500 focus:outline-none focus:ring-4 focus:ring-blue-100";
export default function ScheduleStep({mode,onMode,scheduledAt,onScheduledAt,timezone,onTimezone}:{mode:'NOW'|'LATER';onMode:(value:'NOW'|'LATER')=>void;scheduledAt:string;onScheduledAt:(value:string)=>void;timezone:string;onTimezone:(value:string)=>void}){return <div className="mx-auto max-w-3xl space-y-6"><div><h2 className="text-2xl font-bold">Quando deseja enviar?</h2><p className="text-slate-500">Você pode iniciar agora ou salvar uma data futura.</p></div><div className="grid gap-4 sm:grid-cols-2"><button onClick={()=>onMode('NOW')} className={`rounded-2xl border p-5 text-left ${mode==='NOW'?'border-blue-500 bg-blue-50 text-blue-800':'border-slate-200'}`}><strong>Enviar agora</strong><span className="mt-1 block text-sm">Inicia a preparação e alimenta a fila oficial.</span></button><button onClick={()=>onMode('LATER')} className={`rounded-2xl border p-5 text-left ${mode==='LATER'?'border-blue-500 bg-blue-50 text-blue-800':'border-slate-200'}`}><strong>Data futura</strong><span className="mt-1 block text-sm">Defina horário e fuso horário.</span></button></div>{mode==='LATER'&&<div className="grid gap-4 sm:grid-cols-2"><label>Data e horário<input className={field} type="datetime-local" min={new Date().toISOString().slice(0,16)} value={scheduledAt} onChange={event=>onScheduledAt(event.target.value)}/></label><label>Fuso horário<select className={field} value={timezone} onChange={event=>onTimezone(event.target.value)}><option value="America/Sao_Paulo">Brasília (UTC-3)</option><option value="UTC">UTC</option></select></label></div>}</div>}
