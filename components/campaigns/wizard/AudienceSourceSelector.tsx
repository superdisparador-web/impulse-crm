import { FileSpreadsheet, UserPlus, UsersRound } from "lucide-react";
export type AudienceMode="CSV"|"SEGMENT"|"MANUAL";
const options=[{id:"CSV" as const,title:"Importar CSV",description:"Envie e higienize uma planilha.",icon:FileSpreadsheet},{id:"SEGMENT" as const,title:"Segmentação do CRM",description:"Use filtros e contatos elegíveis.",icon:UsersRound},{id:"MANUAL" as const,title:"Adicionar manualmente",description:"Crie uma lista pequena com segurança.",icon:UserPlus}];
export default function AudienceSourceSelector({value,onChange}:{value:AudienceMode;onChange:(value:AudienceMode)=>void}){return <div role="radiogroup" aria-label="Origem do público" className="grid gap-3 md:grid-cols-3">{options.map(option=>{const Icon=option.icon,selected=value===option.id;return <button role="radio" aria-checked={selected} type="button" key={option.id} onClick={()=>onChange(option.id)} className={`flex min-h-24 items-start gap-3 rounded-2xl border p-4 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 ${selected?"border-blue-500 bg-blue-50 text-blue-900 shadow-sm":"border-slate-200 bg-white text-slate-700 hover:border-blue-200 hover:bg-slate-50"}`}><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${selected?"bg-blue-600 text-white":"bg-slate-100 text-slate-600"}`}><Icon size={20}/></span><span><strong className="block">{option.title}</strong><span className="mt-1 block text-xs opacity-70">{option.description}</span></span></button>})}</div>}
