export default function WizardProgress({steps,current}:{steps:string[];current:number}){return <ol className="flex gap-2 overflow-x-auto pb-2" aria-label="Progresso">{steps.map((label,index)=><li key={label} aria-current={current===index+1?'step':undefined} className={`min-w-36 flex-1 rounded-2xl border p-3 text-sm transition ${current===index+1?'border-blue-500 bg-blue-600 text-white shadow-lg shadow-blue-200':current>index+1?'border-blue-200 bg-blue-50 text-blue-700':'border-slate-200 bg-white text-slate-500'}`}>{index+1}. {label}</li>)}</ol>}
