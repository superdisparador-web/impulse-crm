import { CampaignType } from '@/types/campaign';
import { WhatsappAccount } from '@/types/whatsapp';
export interface CampaignInfoValue{name:string;description:string;campaignType:CampaignType;whatsappAccountId:string;internalNotes:string}
const field='w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-slate-900 shadow-sm focus:border-blue-500 focus:outline-none focus:ring-4 focus:ring-blue-100';
export default function CampaignInfoStep({value,accounts,onChange}:{value:CampaignInfoValue;accounts:WhatsappAccount[];onChange:(value:CampaignInfoValue)=>void}){return <div className="grid gap-4 md:grid-cols-2"><label>Nome *<input autoFocus className={field} value={value.name} onChange={event=>onChange({...value,name:event.target.value})}/></label><label>Conta oficial *<select className={field} value={value.whatsappAccountId} onChange={event=>onChange({...value,whatsappAccountId:event.target.value})}><option value="">Selecione</option>{accounts.map(account=><option key={account.id} value={account.id}>{account.name} · {account.displayPhoneNumber||account.phoneNumber}</option>)}</select></label><label>Categoria *<select className={field} value={value.campaignType} onChange={event=>onChange({...value,campaignType:event.target.value as CampaignType})}><option value="MARKETING">Marketing</option><option value="UTILITY">Utilidade</option><option value="AUTHENTICATION">Autenticação</option></select></label><label className="md:col-span-2">Descrição<textarea className={field} value={value.description} onChange={event=>onChange({...value,description:event.target.value})}/></label><label className="md:col-span-2">Observações internas<textarea className={field} value={value.internalNotes} onChange={event=>onChange({...value,internalNotes:event.target.value})}/></label></div>}
