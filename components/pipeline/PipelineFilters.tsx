import { PipelineFilters as Filters } from "@/types/pipeline-board";

const fields: Array<{ key: keyof Filters; label: string; placeholder: string }> = [
  { key: "search", label: "Busca", placeholder: "Nome, telefone ou e-mail" },
  { key: "development", label: "Empreendimento", placeholder: "Empreendimento" },
  { key: "region", label: "Região", placeholder: "Norte, Sul, Leste, Oeste ou Centro" },
  { key: "neighborhood", label: "Bairro", placeholder: "Bairro" },
  { key: "brokerId", label: "Corretor", placeholder: "ID do corretor" },
  { key: "managerId", label: "Gerente", placeholder: "ID do gerente" },
];

export function PipelineFilters({ value, onChange, onApply, onClear }: { value: Filters; onChange: (filters: Filters) => void; onApply: () => void; onClear: () => void }) {
  return <section aria-label="Filtros do pipeline" className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-6">{fields.map((field) => <label key={field.key} className="grid gap-1 text-xs font-medium text-slate-300"><span>{field.label}</span><input value={String(value[field.key] ?? "")} placeholder={field.placeholder} onChange={(event) => onChange({ ...value, [field.key]: event.target.value })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-sm text-white outline-none focus:border-blue-500" /></label>)}</div><div className="mt-3 grid gap-3 md:grid-cols-3 xl:grid-cols-6"><label className="grid gap-1 text-xs text-slate-300"><span>Status</span><select value={value.status ?? ""} onChange={(event) => onChange({ ...value, status: event.target.value })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"><option value="">Todos</option><option value="NEW">Novo</option><option value="CONTACTED">Contatado</option><option value="QUALIFIED">Qualificado</option><option value="CONVERTED">Convertido</option><option value="LOST">Perdido</option></select></label><label className="grid gap-1 text-xs text-slate-300"><span>Temperatura</span><select value={value.temperature ?? ""} onChange={(event) => onChange({ ...value, temperature: event.target.value })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"><option value="">Todas</option><option value="HOT">Quente</option><option value="WARM">Morno</option><option value="COLD">Frio</option><option value="UNKNOWN">Não definida</option></select></label><label className="grid gap-1 text-xs text-slate-300"><span>Origem</span><input value={value.source ?? ""} onChange={(event) => onChange({ ...value, source: event.target.value })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="grid gap-1 text-xs text-slate-300"><span>SLA</span><select value={value.sla ?? "ALL"} onChange={(event) => onChange({ ...value, sla: event.target.value as Filters["sla"] })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"><option value="ALL">Todos</option><option value="ON_TIME">No prazo</option><option value="OVERDUE">Vencido</option></select></label><label className="grid gap-1 text-xs text-slate-300"><span>De</span><input type="date" value={value.from ?? ""} onChange={(event) => onChange({ ...value, from: event.target.value })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="grid gap-1 text-xs text-slate-300"><span>Até</span><input type="date" value={value.to ?? ""} onChange={(event) => onChange({ ...value, to: event.target.value })} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label></div><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={onClear} className="rounded-lg border border-slate-700 px-4 py-2 text-sm text-slate-200">Limpar</button><button type="button" onClick={onApply} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-500">Aplicar filtros</button></div></section>;
}
