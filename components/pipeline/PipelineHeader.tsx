import { RefreshCw } from "lucide-react";
import { PipelineSummary } from "@/types/pipeline-board";

export function PipelineHeader({ pipelines, selectedPipelineId, loading, refreshing, onSelectPipeline, onRefresh }: { pipelines: PipelineSummary[]; selectedPipelineId: string; loading: boolean; refreshing: boolean; onSelectPipeline: (pipelineId: string) => void; onRefresh: () => void }) {
  return <header className="flex flex-col gap-4 rounded-2xl border border-slate-800 bg-slate-900/70 p-5 lg:flex-row lg:items-end lg:justify-between"><div><h1 className="text-4xl font-bold text-white">Pipeline</h1><p className="mt-2 text-slate-400">Acompanhe e movimente seus leads pelo funil de vendas</p></div><div className="flex flex-col gap-3 sm:flex-row sm:items-end"><label className="grid gap-2 text-sm text-slate-300"><span>Selecionar pipeline</span><select aria-label="Selecionar pipeline" className="min-w-64 rounded-lg border border-slate-700 bg-slate-950 p-3 text-white outline-none focus:border-blue-500" disabled={loading || pipelines.length === 0} value={selectedPipelineId} onChange={(event) => onSelectPipeline(event.target.value)}><option value="">Selecione</option>{pipelines.map((pipeline) => <option key={pipeline.id} value={pipeline.id}>{pipeline.name}{pipeline.isDefault ? " • padrão" : ""}</option>)}</select></label><button type="button" aria-label="Atualizar dados do pipeline" onClick={onRefresh} disabled={loading || refreshing || !selectedPipelineId} className="inline-flex items-center justify-center gap-2 rounded-lg bg-blue-600 px-4 py-3 font-semibold text-white transition hover:bg-blue-700 disabled:cursor-not-allowed disabled:opacity-60"><RefreshCw size={18} className={refreshing ? "animate-spin" : ""} />{refreshing ? "Atualizando" : "Atualizar"}</button></div></header>;
}
