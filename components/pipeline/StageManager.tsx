"use client";

import { useEffect, useState } from "react";
import { ArrowDown, ArrowUp, Pencil, Plus, Settings2, Trash2, X } from "lucide-react";
import { PipelineStage } from "@/types/pipeline-board";

export function StageManager({ stages, saving, error, onCreate, onUpdate, onDelete, onReorder }: { stages: PipelineStage[]; saving: boolean; error?: string; onCreate: (name: string, color: string) => Promise<void>; onUpdate: (stage: PipelineStage, name: string, color: string) => Promise<void>; onDelete: (stage: PipelineStage) => Promise<void>; onReorder: (stages: PipelineStage[]) => Promise<void> }) {
  const [open, setOpen] = useState(false); const [editing, setEditing] = useState<PipelineStage | null>(null); const [name, setName] = useState(""); const [color, setColor] = useState("#3b82f6");
  const begin = (stage?: PipelineStage) => { setEditing(stage ?? null); setName(stage?.name ?? ""); setColor(stage?.color ?? "#3b82f6"); };
  const move = (index: number, offset: number) => { const next = [...stages]; const destination = index + offset; if (destination < 0 || destination >= next.length) return; [next[index], next[destination]] = [next[destination], next[index]]; void onReorder(next); };
  useEffect(() => { if (!open) return; const close = (event: KeyboardEvent) => { if (event.key === "Escape") setOpen(false); }; window.addEventListener("keydown", close); return () => window.removeEventListener("keydown", close); }, [open]);
  return <><button type="button" onClick={() => { setOpen(true); begin(); }} className="inline-flex items-center gap-2 rounded-xl border border-slate-700 bg-slate-900 px-4 py-2.5 text-sm font-semibold text-slate-100 shadow-lg transition hover:border-blue-500 hover:text-blue-200"><Settings2 size={17} />Gerenciar etapas</button>{open && <div role="dialog" aria-modal="true" aria-label="Gerenciamento das etapas" className="fixed inset-0 z-[80] grid place-items-center bg-slate-950/80 p-4 backdrop-blur-sm"><button aria-label="Fechar gerenciamento de etapas" className="absolute inset-0" onClick={() => setOpen(false)} /><section className="relative z-10 flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl border border-slate-700 bg-slate-900 shadow-2xl"><header className="flex items-center justify-between border-b border-slate-800 p-5"><div><h2 className="text-xl font-bold text-white">Gerenciar etapas</h2><p className="mt-1 text-sm text-slate-400">Configure nomes, cores e a ordem do seu funil.</p></div><button aria-label="Fechar" onClick={() => setOpen(false)} className="rounded-lg p-2 text-slate-400 hover:bg-slate-800 hover:text-white"><X size={20} /></button></header><div className="overflow-y-auto p-5"><form onSubmit={(event) => { event.preventDefault(); void (async () => { if (editing) await onUpdate(editing, name.trim(), color); else await onCreate(name.trim(), color); begin(); })(); }} className="grid gap-3 rounded-2xl border border-slate-800 bg-slate-950/70 p-4 sm:grid-cols-[auto_1fr_auto]"><input aria-label="Cor da etapa" type="color" value={color} onChange={(event) => setColor(event.target.value)} className="h-11 w-full rounded-lg border border-slate-700 bg-slate-950 p-1 sm:w-14" /><input autoFocus aria-label="Nome da etapa" value={name} maxLength={120} onChange={(event) => setName(event.target.value)} placeholder="Ex.: Proposta enviada" className="rounded-lg border border-slate-700 bg-slate-900 px-3 py-2 text-white outline-none focus:border-blue-500" /><button disabled={saving || !name.trim()} className="inline-flex items-center justify-center gap-2 rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50"><Plus size={16} />{editing ? "Salvar" : "Adicionar"}</button></form>{error && <p role="alert" className="mt-3 rounded-lg bg-red-950/60 p-3 text-sm text-red-200">{error}</p>}<ul className="mt-5 space-y-2">{stages.map((stage, index) => <li key={stage.id} className={`flex items-center gap-3 rounded-xl border p-3 transition ${editing?.id === stage.id ? "border-blue-500 bg-blue-950/20" : "border-slate-800 bg-slate-950"}`}><span className="h-4 w-4 rounded-full" style={{ backgroundColor: stage.color ?? "#3b82f6" }} /><div className="min-w-0 flex-1"><p className="truncate font-medium text-slate-100">{stage.name}</p><p className="text-xs text-slate-500">{stage.total ?? stage.cards.length} leads</p></div><button aria-label={`Subir ${stage.name}`} disabled={saving || index === 0} onClick={() => move(index, -1)} className="rounded-lg p-2 hover:bg-slate-800 disabled:opacity-25"><ArrowUp size={16} /></button><button aria-label={`Descer ${stage.name}`} disabled={saving || index === stages.length - 1} onClick={() => move(index, 1)} className="rounded-lg p-2 hover:bg-slate-800 disabled:opacity-25"><ArrowDown size={16} /></button><button aria-label={`Editar ${stage.name}`} onClick={() => begin(stage)} className="rounded-lg p-2 text-blue-300 hover:bg-blue-950"><Pencil size={16} /></button><button aria-label={`Excluir ${stage.name}`} disabled={saving || stage.cards.length > 0 || Boolean(stage.total)} onClick={() => { if (window.confirm(`Excluir a etapa “${stage.name}”?`)) void onDelete(stage); }} className="rounded-lg p-2 text-red-300 hover:bg-red-950 disabled:opacity-25"><Trash2 size={16} /></button></li>)}</ul></div></section></div>}</>;
}
