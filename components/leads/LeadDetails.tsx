import { Lead } from '@/types/lead';
import LeadHistory from './LeadHistory';
export default function LeadDetails({ lead, onClose }: { lead: Lead; onClose: () => void }) { return <div className="fixed inset-0 z-40 flex items-center justify-center bg-black/60 p-4"><div className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-xl bg-slate-900 p-6"><div className="mb-4 flex items-center justify-between"><h2 className="text-2xl font-bold">{lead.name}</h2><button onClick={onClose} className="rounded bg-slate-700 px-3 py-2">Fechar</button></div><div className="grid gap-3 md:grid-cols-2"><p><b>Telefone:</b> {lead.phone}</p><p><b>E-mail:</b> {lead.email || '-'}</p><p><b>Origem:</b> {lead.source}</p><p><b>Status:</b> {lead.status}</p><p><b>Temperatura:</b> {lead.temperature}</p><p><b>Responsável:</b> {lead.assignedUser?.name || 'Sem responsável'}</p><p><b>Criado em:</b> {new Date(lead.createdAt).toLocaleString('pt-BR')}</p></div><div className="mt-4"><b>Observações:</b><p className="mt-1 whitespace-pre-wrap text-slate-300">{lead.notes || '-'}</p></div><h3 className="mt-6 mb-3 text-lg font-semibold">Histórico</h3><LeadHistory history={lead.history} /></div></div>; }
