import { Lead } from '@/types/lead';
import LeadHistory from './LeadHistory';
export default function LeadDetails({ lead, onClose }: { lead: Lead; onClose: () => void }) { return <div className="fixed inset-0 z-40 flex items-center justify-center bg-black/60 p-4"><div className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-xl bg-slate-900 p-6"><div className="mb-4 flex items-center justify-between"><h2 className="text-2xl font-bold">{lead.name || "Lead sem nome"}</h2><button onClick={onClose} className="rounded bg-slate-700 px-3 py-2">Fechar</button></div><div className="grid gap-3 md:grid-cols-2"><p><b>Telefone:</b> {lead.phone || "-"}</p><p><b>E-mail:</b> {lead.email || '-'}</p><p><b>Origem:</b> {lead.source}</p><p><b>Status:</b> {lead.status}</p><p><b>Temperatura:</b> {lead.temperature}</p><p><b>Score:</b> {lead.score}</p><p><b>Organização:</b> {lead.organization?.name || '-'}</p><p><b>Corretor:</b> {lead.assignedUser?.name || 'Sem responsável'}</p><p><b>Gerente:</b> {lead.managerUser?.name || '-'}</p><p><b>Criado em:</b> {new Date(lead.createdAt).toLocaleString('pt-BR')}</p></div><div className="mt-4"><b>Observações:</b><p className="mt-1 whitespace-pre-wrap text-slate-300">{lead.notes || '-'}</p></div><h3 className="mt-6 mb-3 text-lg font-semibold">Eventos do Lead</h3><LeadHistory history={lead.events} /><h3 className="mt-6 mb-3 text-lg font-semibold">Atividades</h3><div className="space-y-2">{lead.activities?.length ? lead.activities.map((activity) => <div key={activity.id} className="rounded-lg border border-slate-800 p-3"><p className="font-medium">{activity.title}</p><p className="text-xs text-slate-400">{activity.status} • {activity.priority} • {new Date(activity.dueAt).toLocaleString('pt-BR')} • {activity.responsibleUser?.name}</p>{activity.note && <p className="mt-1 text-sm text-slate-300">{activity.note}</p>}</div>) : <p className="text-sm text-slate-400">Sem atividades.</p>}</div></div></div>; }
