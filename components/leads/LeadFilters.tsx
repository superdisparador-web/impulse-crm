import { LeadListParams } from '@/types/lead';
import { Organization } from '@/types/organization';
import { User } from '@/types/user';
export default function LeadFilters({ filters, organizations, users, onChange }: { filters: LeadListParams; organizations: Organization[]; users: User[]; onChange: (filters: LeadListParams) => void }) { const set = (key: keyof LeadListParams, value: string) => onChange({ ...filters, page: 1, [key]: value }); return <div className="grid gap-3 md:grid-cols-3 xl:grid-cols-6"><select className="rounded-lg bg-slate-900 p-3" value={filters.status ?? ''} onChange={(e) => set('status', e.target.value)}><option value="">Status</option>{['NEW','ASSIGNED','CONTACT_PENDING','IN_CONTACT','QUALIFIED','UNQUALIFIED','CONVERTED','LOST','ARCHIVED'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.temperature ?? ''} onChange={(e) => set('temperature', e.target.value)}><option value="">Temperatura</option>{['COLD','WARM','HOT','UNKNOWN'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.source ?? ''} onChange={(e) => set('source', e.target.value)}><option value="">Origem</option>{['MANUAL','CSV_IMPORT','META_ADS','WEBSITE','PUBLIC_API','WEBHOOK','REFERRAL','ORGANIC','FACEBOOK','INSTAGRAM','LANDING_PAGE','PHONE','OTHER'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.organizationId ?? ''} onChange={(e) => set('organizationId', e.target.value)}><option value="">Organização</option>{organizations.map((o) => <option key={o.id} value={o.id}>{o.name}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.assignedUserId ?? ''} onChange={(e) => set('assignedUserId', e.target.value)}><option value="">Responsável</option>{users.map((u) => <option key={u.id} value={u.id}>{u.name}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.assigned === true ? 'true' : filters.assigned === false ? 'false' : ''} onChange={(e) => onChange({ ...filters, page: 1, assigned: e.target.value === '' ? '' : e.target.value === 'true' })}><option value="">Atribuição</option><option value="true">Atribuídos</option><option value="false">Não atribuídos</option></select></div>; }
