import { LeadListParams } from '@/types/lead';
import { User } from '@/types/user';
export default function LeadFilters({ filters, users, onChange }: { filters: LeadListParams; users: User[]; onChange: (filters: LeadListParams) => void }) { const set = (key: keyof LeadListParams, value: string) => onChange({ ...filters, page: 1, [key]: value }); return <div className="grid gap-3 md:grid-cols-3 xl:grid-cols-5"><select className="rounded-lg bg-slate-900 p-3" value={filters.status ?? ''} onChange={(e) => set('status', e.target.value)}><option value="">Status</option>{['NEW','CONTACTED','QUALIFIED','UNQUALIFIED','CONVERTED','LOST'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.temperature ?? ''} onChange={(e) => set('temperature', e.target.value)}><option value="">Temperatura</option>{['COLD','WARM','HOT'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.source ?? ''} onChange={(e) => set('source', e.target.value)}><option value="">Origem</option>{['MANUAL','IMPORT','WHATSAPP','CAMPAIGN','FACEBOOK','INSTAGRAM','LANDING_PAGE','REFERRAL','PHONE','OTHER'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.assignedUserId ?? ''} onChange={(e) => set('assignedUserId', e.target.value)}><option value="">Responsável</option>{users.map((u) => <option key={u.id} value={u.id}>{u.name}</option>)}</select><select className="rounded-lg bg-slate-900 p-3" value={filters.assigned === true ? 'true' : filters.assigned === false ? 'false' : ''} onChange={(e) => onChange({ ...filters, page: 1, assigned: e.target.value === '' ? '' : e.target.value === 'true' })}><option value="">Atribuição</option><option value="true">Atribuídos</option><option value="false">Não atribuídos</option></select></div>; }
