"use client";
import { useEffect, useRef, useState } from "react";
import { leadService } from "@/services/lead.service";
import { Lead, LeadFormData, LeadSource, LeadStatus, LeadTemperature } from "@/types/lead";
import { User } from "@/types/user";
import { leadSourceLabels, leadStatusLabels, leadTemperatureLabels } from "./lead-labels";

const sources = Object.keys(leadSourceLabels) as LeadSource[];
const statuses = Object.keys(leadStatusLabels) as LeadStatus[];
const temperatures = Object.keys(leadTemperatureLabels) as LeadTemperature[];
type Props = { lead?: Lead | null; users: User[]; onSuccess: (lead: Lead) => void; onCancel: () => void };
const onlyDigits = (value?: string | null) => value?.replace(/\D/g, "") || "";

export default function LeadForm({ lead, users, onSuccess, onCancel }: Props) {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");
  const nameRef = useRef<HTMLInputElement>(null);
  const [form, setForm] = useState<LeadFormData>({ name: lead?.name ?? "", phone: lead?.phone ?? "", email: lead?.email ?? "", document: lead?.document ?? "", source: lead?.source ?? "MANUAL", status: lead?.status ?? "NEW", temperature: lead?.temperature ?? "UNKNOWN", assignedUserId: lead?.assignedUserId ?? "", managerUserId: lead?.managerUserId ?? "", notes: lead?.notes ?? "" });
  useEffect(() => { nameRef.current?.focus(); const onKey = (event: KeyboardEvent) => { if (event.key === "Escape" && !loading) onCancel(); }; window.addEventListener("keydown", onKey); return () => window.removeEventListener("keydown", onKey); }, [loading, onCancel]);
  function update(field: keyof LeadFormData, value: string) { setForm((current) => ({ ...current, [field]: value })); }
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (loading) return; const email = form.email?.trim() ?? ""; if (!form.name?.trim() && !form.phone?.trim() && !email) { setError("Informe pelo menos nome, telefone ou e-mail."); return; } if (email && !/^\S+@\S+\.\S+$/.test(email)) { setError("Informe um e-mail válido."); return; } setLoading(true); setError(""); const payload: LeadFormData = { ...form, name: form.name?.trim() || null, phone: onlyDigits(form.phone) || null, email: email || null, document: onlyDigits(form.document) || null, assignedUserId: form.assignedUserId || null, managerUserId: form.managerUserId || null, notes: form.notes?.trim() || null }; try { const saved = lead?.id ? await leadService.update(lead.id, payload) : await leadService.create(payload); onSuccess(saved); } catch (err) { setError(err instanceof Error ? err.message : "Erro ao salvar lead."); } finally { setLoading(false); } }
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4" role="dialog" aria-modal="true" aria-labelledby="lead-form-title"><button aria-label="Fechar formulário" className="absolute inset-0" disabled={loading} onClick={onCancel} /><form onSubmit={submit} className="relative max-h-[90vh] w-full max-w-3xl space-y-4 overflow-y-auto rounded-xl bg-slate-900 p-6"><div><h2 id="lead-form-title" className="text-2xl font-bold">{lead ? "Editar lead" : "Novo Lead"}</h2><p className="text-sm text-slate-400">Informe pelo menos nome, telefone ou e-mail.</p></div>{error && <div className="rounded-lg border border-red-800 bg-red-950/50 p-3 text-sm text-red-200">{error}</div>}<div className="grid gap-4 md:grid-cols-2"><label className="space-y-1 text-sm">Nome<input ref={nameRef} className="w-full rounded-lg bg-slate-800 p-3" value={form.name ?? ""} onChange={(e) => update("name", e.target.value)} /></label><label className="space-y-1 text-sm">Telefone<input className="w-full rounded-lg bg-slate-800 p-3" value={form.phone ?? ""} onChange={(e) => update("phone", e.target.value)} /></label><label className="space-y-1 text-sm">E-mail<input className="w-full rounded-lg bg-slate-800 p-3" type="email" value={form.email ?? ""} onChange={(e) => update("email", e.target.value)} /></label><label className="space-y-1 text-sm">CPF/documento<input className="w-full rounded-lg bg-slate-800 p-3" value={form.document ?? ""} onChange={(e) => update("document", e.target.value)} /></label><label className="space-y-1 text-sm">Origem<select className="w-full rounded-lg bg-slate-800 p-3" value={form.source} onChange={(e) => update("source", e.target.value)}>{sources.map((item) => <option key={item} value={item}>{leadSourceLabels[item]}</option>)}</select></label><label className="space-y-1 text-sm">Status<select className="w-full rounded-lg bg-slate-800 p-3" value={form.status} onChange={(e) => update("status", e.target.value)}>{statuses.map((item) => <option key={item} value={item}>{leadStatusLabels[item]}</option>)}</select></label><label className="space-y-1 text-sm">Temperatura<select className="w-full rounded-lg bg-slate-800 p-3" value={form.temperature} onChange={(e) => update("temperature", e.target.value)}>{temperatures.map((item) => <option key={item} value={item}>{leadTemperatureLabels[item]}</option>)}</select></label><label className="space-y-1 text-sm">Corretor responsável<select className="w-full rounded-lg bg-slate-800 p-3" value={form.assignedUserId ?? ""} onChange={(e) => update("assignedUserId", e.target.value)}><option value="">Sem corretor</option>{users.map((user) => <option key={user.id} value={user.id}>{user.name}</option>)}</select></label></div><label className="block space-y-1 text-sm">Observação inicial<textarea className="w-full rounded-lg bg-slate-800 p-3" rows={4} value={form.notes ?? ""} onChange={(e) => update("notes", e.target.value)} /></label><div className="flex justify-end gap-3"><button type="button" disabled={loading} onClick={onCancel} className="rounded-lg bg-slate-700 px-5 py-3 disabled:opacity-60">Cancelar</button><button disabled={loading} className="rounded-lg bg-blue-600 px-5 py-3 disabled:opacity-60">{loading ? "Salvando..." : "Salvar"}</button></div></form></div>;
}
