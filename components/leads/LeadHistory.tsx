import { LeadHistory as LeadHistoryType } from '@/types/lead';
export default function LeadHistory({ history = [] }: { history?: LeadHistoryType[] }) { return <div className="space-y-3">{history.length === 0 ? <p className="text-sm text-slate-400">Sem histórico.</p> : history.map((item) => <div key={item.id} className="rounded-lg border border-slate-800 p-3"><p className="font-medium">{item.description}</p><p className="text-xs text-slate-400">{item.action} • {new Date(item.createdAt).toLocaleString('pt-BR')}{item.performedByUser ? ` • ${item.performedByUser.name}` : ''}</p></div>)}</div>; }
