"use client";
import { useEffect, useState } from "react";
import { Download, ShieldCheck } from "lucide-react";
import { reportsService, ReportFilters } from "@/services/reports.service";
import { campaignsService } from "@/services/campaigns.service";
import { templatesService } from "@/services/templates.service";
import { userService } from "@/services/user.service";
import { Campaign } from "@/types/campaign";
import { User } from "@/types/user";
import { WhatsappTemplate } from "@/types/templates";
import { AnalyticsCard, AnalyticsHeader, ErrorState } from "@/components/analytics/AnalyticsUi";
const sourceLabels: Record<string, string> = { MANUAL: "Cadastro manual", IMPORT: "Importação de arquivo", WEBSITE: "Site", PUBLIC_API: "Sistema conectado", WEBHOOK: "Automação", REFERRAL: "Indicação", ORGANIC: "Busca orgânica", FACEBOOK: "Facebook", INSTAGRAM: "Instagram", LANDING_PAGE: "Página de campanha", PHONE: "Telefone", OTHER: "Outros" };
const campaignStatusLabels: Record<string, string> = { DRAFT: "Rascunho", VALIDATING: "Em preparação", READY: "Pronta", SCHEDULED: "Agendada", QUEUED: "Na fila", RUNNING: "Em andamento", PAUSED: "Pausada", COMPLETED: "Concluída", COMPLETED_WITH_ERRORS: "Concluída com pendências", CANCELED: "Cancelada", FAILED: "Não concluída" };
const initial: ReportFilters = { dataset: "campaigns", campaignId: "", managerId: "", brokerId: "", templateId: "", from: "", to: "", source: "", status: "", product: "", development: "" };
export default function ReportsPage() { const [filters, setFilters] = useState(initial), [campaigns, setCampaigns] = useState<Campaign[]>([]), [users, setUsers] = useState<User[]>([]), [templates, setTemplates] = useState<WhatsappTemplate[]>([]), [downloading, setDownloading] = useState(false), [error, setError] = useState(""); useEffect(() => { Promise.all([campaignsService.getCampaigns({ limit: 100 }), userService.getAll({ limit: 100 }), templatesService.getTemplates({ pageSize: 100 })]).then(([campaignData, userData, templateData]) => { setCampaigns(campaignData.items); setUsers(userData.items); setTemplates(templateData.items); }).catch(() => setError("Alguns filtros não puderam ser carregados.")); }, []); const set = (key: keyof ReportFilters, value: string) => setFilters((current) => ({ ...current, [key]: value })); async function download() { setDownloading(true); setError(""); try { await reportsService.downloadCsv(filters); } catch { setError("Não foi possível preparar o relatório. Verifique sua conexão e tente novamente."); } finally { setDownloading(false); } } const field = "rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm"; return <main className="space-y-6"><AnalyticsHeader eyebrow="Visão gerencial" title="Central de relatórios" description="Combine filtros comerciais e baixe informações organizadas para acompanhar sua operação." actions={<div className="flex items-center gap-2 rounded-2xl border border-white/10 bg-white/10 px-4 py-3 text-sm"><ShieldCheck/>Arquivo preparado com segurança</div>}/>{error && <ErrorState message={error}/>}<AnalyticsCard title="Configuração do relatório"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><label className="text-xs font-bold text-slate-500">Relatório<select value={filters.dataset} onChange={(event) => set("dataset", event.target.value)} className={`mt-1 w-full ${field}`}><option value="campaigns">Campanhas</option><option value="events">Eventos</option><option value="conversions">Conversões</option></select></label><label className="text-xs font-bold text-slate-500">Campanha<select value={filters.campaignId} onChange={(event) => set("campaignId", event.target.value)} className={`mt-1 w-full ${field}`}><option value="">Todas</option>{campaigns.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Modelo de mensagem<select value={filters.templateId} onChange={(event) => set("templateId", event.target.value)} className={`mt-1 w-full ${field}`}><option value="">Todos</option>{templates.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Corretor<select value={filters.brokerId} onChange={(event) => set("brokerId", event.target.value)} className={`mt-1 w-full ${field}`}><option value="">Todos</option>{users.filter((user) => ["BROKER", "CORRETOR"].includes(user.role)).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Gerente<select value={filters.managerId} onChange={(event) => set("managerId", event.target.value)} className={`mt-1 w-full ${field}`}><option value="">Todos</option>{users.filter((user) => user.role === "MANAGER").map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Origem<select value={filters.source} onChange={(event) => set("source", event.target.value)} className={`mt-1 w-full ${field}`}><option value="">Todas</option>{Object.entries(sourceLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Situação{filters.dataset === "campaigns" ? <select value={filters.status} onChange={(event) => set("status", event.target.value)} className={`mt-1 w-full ${field}`}><option value="">Todos</option>{Object.entries(campaignStatusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select> : <input value={filters.status} onChange={(event) => set("status", event.target.value)} placeholder="Ex.: mensagem enviada" className={`mt-1 w-full ${field}`}/>}</label><label className="text-xs font-bold text-slate-500">Produto<input value={filters.product} onChange={(event) => set("product", event.target.value)} placeholder="Produto" className={`mt-1 w-full ${field}`}/></label><label className="text-xs font-bold text-slate-500">Empreendimento<input value={filters.development} onChange={(event) => set("development", event.target.value)} placeholder="Empreendimento" className={`mt-1 w-full ${field}`}/></label><label className="text-xs font-bold text-slate-500">Data inicial<input type="date" value={filters.from} onChange={(event) => set("from", event.target.value)} className={`mt-1 w-full ${field}`}/></label><label className="text-xs font-bold text-slate-500">Data final<input type="date" value={filters.to} onChange={(event) => set("to", event.target.value)} className={`mt-1 w-full ${field}`}/></label></div><div className="mt-6 flex flex-wrap gap-3"><button disabled={downloading} onClick={() => void download()} className="flex items-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white shadow-lg shadow-blue-200 hover:bg-blue-700 disabled:opacity-50"><Download size={18}/>{downloading ? "Preparando..." : "Baixar relatório"}</button><button onClick={() => setFilters(initial)} className="rounded-xl px-5 py-3 text-sm font-bold text-slate-600 hover:bg-slate-100">Limpar filtros</button></div></AnalyticsCard></main>; }

